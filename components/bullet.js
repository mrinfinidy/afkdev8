import { Box } from '@chakra-ui/react'
import styled from '@emotion/styled'

export const BulletSection = styled(Box) `
    padding-left: 3.4em;
    text-indent: -3.4em;
`

export const BulletPoint = styled.span`
    font-weight: bold;
    margin-right: 1em;
`
